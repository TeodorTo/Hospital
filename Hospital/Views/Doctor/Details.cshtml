@model Hospital.Models.DoctorDetailsViewModel

<h2>Doctor Details</h2>

<div>
    <dl class="row">
        <dt class="col-sm-2">ID:</dt>
        <dd class="col-sm-10">@Model.Id</dd>

        <dt class="col-sm-2">Name:</dt>
        <dd class="col-sm-10">@Model.Name</dd>

        <dt class="col-sm-2">Specialty:</dt>
        <dd class="col-sm-10">@Model.Specialty</dd>

        <dt class="col-sm-2">Department:</dt>
        <dd class="col-sm-10">@Model.DepartmentName</dd>

        <dt class="col-sm-2">Office:</dt>
        <dd class="col-sm-10">@Model.Office</dd>

        <dt class="col-sm-2">Working Hours:</dt>
        <dd class="col-sm-10">@Model.WorkingHours</dd>
    </dl>
</div>

<a asp-action="Index">Back to List</a>
